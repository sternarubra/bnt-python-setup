[options]
required_plugins = [
    "pytest-asyncio",
	"pytest-cov",
	'pytest-mock',
]
addopts = "-Werror --cov=lacbackends --cov-report term-missing:skip-covered --no-cov-on-fail"
testpaths = [
	'tests',
]
	
[coverage:run]
branch      = true
context     = "lacbackends"
cover_pylib = false

[coverage:report]
exclude_lines = [
    "pragma: no cover",
	"if __name__ == .__main__.:",
]
